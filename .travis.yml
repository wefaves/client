#.travis.yml

language: node_js
node_js:
  - "6.9.1"

before_script:
  - npm install -g firebase-tools
  - npm install -g @angular/cli

script:
  - npm install
  - ng build --prod

after_success:
  - firebase deploy --token $FIREBASE_TOKEN
